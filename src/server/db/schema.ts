/**
 * Drizzle schema.
 *
 * Intentionally empty. See tasks.md (Phase 1) and docs/spec.md section 25
 * for the data model: user, brand, asset, post, slide, template, plus the
 * Better Auth tables.
 */
export {};
